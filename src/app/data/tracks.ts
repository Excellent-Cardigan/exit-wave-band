// Content is now served from Kirby CMS at /cms/tracks.json
// Use useKirbyData<KirbyTrack[]>('tracks.json') in components.
// This file keeps the Track type alias for backwards-compatible imports.
export type { KirbyTrack as Track } from '../types/kirby';
